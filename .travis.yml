sudo: required
services:
    - docker

before_install:
    - docker build -t jimandrews/docker-react -f Dockerfile.dev .

script:
    - docker run -e CI=true jimandrews/docker-react npm run test

language: generic